import { View, Text } from "react-native";

const AccountStack = ({navigation}) => {
    return (
        <View style={{minHeight: "100%"}}>
            <Text>Account Stack</Text>
        </View>
    );
}
 
export default AccountStack;