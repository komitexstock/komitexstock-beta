import { View, Text } from "react-native";

const InventoryStack = ({navigation}) => {
    return (
        <View style={{minHeight: "100%", display: 'flex', justifyContent: 'center', alignItems: 'center'}}>
            <Text>Inventory Stack</Text>
        </View>
    );
}
 
export default InventoryStack;