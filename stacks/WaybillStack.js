import { View, Text } from "react-native";

const WaybillStack = ({navigation}) => {
    return (
        <View style={{minHeight: "100%"}}>
            <Text>Waybill Stack</Text>
        </View>
    );
}
 
export default WaybillStack;